# قاعدة المعرفة الشاملة — oalsuhail

---

## أولاً: المستودعات (Repositories)

### الوضع الحالي
| المستودع | المحتوى | الحالة |
|---------|---------|-------|
| `oalsuhail/-B` | موقع React + أداة Python في مستودع واحد | يحتاج فصل |

### الخطة المقررة (لم تُنفَّذ بعد)
```
oalsuhail/studio-site      ← موقع React/Framer Motion
oalsuhail/website-agent    ← CLI بايثون لتوليد مواقع
```

### مشاكل يجب إصلاحها
- اسم الريبو `-B` غير واضح → يُغيَّر عند الفصل
- لا يوجد `main` branch → يُنشأ كـ default
- الـ default branch الحالي هو `claude/add-skills-feature-P9NQA`

---

## ثانياً: تنظيم المحادثات

### القاعدة الذهبية
```
محادثة مفيدة → جملة واحدة في CLAUDE.md
لا تحفظ المحادثة كاملة — الضجيج يضر أكثر مما ينفع
```

### هيكل التوثيق في كل مشروع
```
my-project/
├── CLAUDE.md          ← ذاكرة Claude (بنية + قرارات + ملفات مرتبطة)
├── KNOWLEDGE_BASE.md  ← هذا الملف (مهارات + موصلات + خطة)
└── src/
```

### نموذج إضافة قرار من محادثة
```markdown
### اسم الموضوع
- **قرار:** ما تم الاتفاق عليه (جملة واحدة)
- **السبب:** لماذا (اختياري، جملة واحدة)
```

---

## ثالثاً: المهارات (Skills) المتاحة

تُستدعى بكتابة `/اسم-المهارة` في المحادثة.

### مهارات التصميم

| المهارة | الأمر | متى تستخدمها |
|---------|------|-------------|
| **ui-ux-pro-max** | `/ui-ux-pro-max` | بناء واجهات كاملة — مواقع، لوحات تحكم، تطبيقات. 67 أسلوب، 96 لوحة ألوان |
| **design** | `/design` | شامل — شعارات (55 أسلوب)، هوية بصرية، بانرات، أيقونات، صور سوشيال |
| **ui-styling** | `/ui-styling` | shadcn/ui + Tailwind، مكونات accessible، dark mode |
| **design-system** | `/design-system` | نظام tokens (primitive→semantic→component)، CSS variables، مواصفات مكونات |
| **banner-design** | `/banner-design` | بانرات سوشيال ميديا وإعلانات — Facebook، LinkedIn، YouTube، Instagram |
| **brand** | `/brand` | هوية العلامة التجارية، أسلوب الكتابة، دليل الأسلوب |
| **slides** | `/slides` | عروض HTML تفاعلية مع Chart.js |

### مهارات التطوير والإعداد

| المهارة | الأمر | متى تستخدمها |
|---------|------|-------------|
| **claude-api** | `/claude-api` | بناء تطبيقات على Anthropic SDK، prompt caching، tool use |
| **init** | `/init` | إنشاء CLAUDE.md لأي مشروع جديد تلقائياً |
| **review** | `/review` | مراجعة Pull Request كاملة |
| **security-review** | `/security-review` | مراجعة أمنية للكود |
| **simplify** | `/simplify` | مراجعة الكود وإزالة التعقيد الزائد |
| **update-config** | `/update-config` | تعديل إعدادات Claude Code (permissions، hooks، env vars) |
| **session-start-hook** | `/session-start-hook` | إعداد hook يعمل عند بدء كل جلسة |
| **fewer-permission-prompts** | `/fewer-permission-prompts` | تقليل طلبات الأذونات المتكررة |
| **loop** | `/loop` | تشغيل مهمة بشكل دوري (مثلاً كل 5 دقائق) |
| **keybindings-help** | `/keybindings-help` | تخصيص اختصارات لوحة المفاتيح |

---

## رابعاً: الموصلات (MCP Servers) المتاحة

موصلات تعطي Claude صلاحية التفاعل مع خدمات خارجية مباشرة.

### GitHub
```
الصلاحيات: قراءة وكتابة كاملة
```
| ما يمكن فعله |
|-------------|
| إنشاء/قراءة PRs والـ issues |
| رفع ملفات وإنشاء branches |
| مراجعة الكود والتعليق |
| مراقبة CI/CD |
| البحث في الكود والمستودعات |

**المستودع المسموح به:** `oalsuhail/-B` فقط حالياً

---

### Gmail (البريد الإلكتروني)
```
الصلاحيات: قراءة وكتابة
```
| ما يمكن فعله |
|-------------|
| البحث في رسائل البريد |
| إنشاء مسودات |
| إدارة الـ labels والتصنيفات |

**متى تستخدمه:** أتمتة الردود، تصنيف الرسائل، إنشاء مسودات تلقائية

---

### Google Calendar (التقويم)
```
الصلاحيات: قراءة وكتابة
```
| ما يمكن فعله |
|-------------|
| إنشاء وتعديل وحذف أحداث |
| عرض الجداول والتقويمات |
| اقتراح أوقات مناسبة للاجتماعات |
| الرد على دعوات الاجتماعات |

**متى تستخدمه:** جدولة مهام المشروع، تنظيم الاجتماعات تلقائياً

---

### File System (نظام الملفات)
```
الصلاحيات: قراءة وكتابة
```
| ما يمكن فعله |
|-------------|
| قراءة ونسخ وإنشاء ملفات |
| البحث داخل الملفات |
| تحميل محتوى الملفات |
| عرض metadata والصلاحيات |

---

### Business/Prospects Enrichment
```
الصلاحيات: قراءة وتصدير
```
| ما يمكن فعله |
|-------------|
| البحث عن شركات وأفراد |
| تفصيل بيانات العملاء المحتملين |
| تصدير بيانات إلى CSV |
| إحصاءات وتحليلات |

**متى تستخدمه:** أبحاث السوق، العثور على عملاء محتملين

---

### Experiences
```
الصلاحيات: قراءة
```
| ما يمكن فعله |
|-------------|
| البحث في التجارب والخبرات |
| عرض تفاصيل أي تجربة |

---

## خامساً: ربط المهارات والموصلات بالمشاريع

### مشروع: studio-site (موقع React)
```
المهارات المناسبة:
  /ui-ux-pro-max   ← لتصميم sections جديدة
  /design-system   ← لتوسيع نظام الـ tokens
  /ui-styling      ← إضافة shadcn/ui components
  /review          ← مراجعة الكود قبل الدمج

الموصلات المناسبة:
  GitHub           ← PRs، branches، CI
  File System      ← إدارة الملفات
```

### مشروع: website-agent (أداة Python)
```
المهارات المناسبة:
  /claude-api      ← تطوير الـ agent وتحسين الـ prompts
  /security-review ← فحص أمني (path traversal، API keys)
  /simplify        ← تنظيف الكود
  /review          ← مراجعة PRs

الموصلات المناسبة:
  GitHub           ← PRs، branches
  File System      ← إدارة ملفات الـ output
```

### أي مشروع جديد — خطوات البداية
```bash
# 1. أنشئ المشروع
git init my-project && cd my-project

# 2. ولّد CLAUDE.md تلقائياً
/init

# 3. أضف المهارات المناسبة من القائمة أعلاه
# 4. وثّق الموصلات التي ستستخدمها في CLAUDE.md
```

---

## سادساً: خلاصة سريعة

```
أريد تصميم واجهة       → /ui-ux-pro-max
أريد شعاراً أو هوية    → /design
أريد بانر سوشيال       → /banner-design
أريد عرض تقديمي       → /slides
أريد مراجعة كود        → /review
أريد فحص أمني          → /security-review
أريد تبسيط كود         → /simplify
أريد Claude API         → /claude-api
أريد مشروع جديد        → /init
أريد تعديل إعدادات     → /update-config

أريد GitHub             → MCP GitHub (تلقائي)
أريد بريد إلكتروني     → MCP Gmail
أريد تقويم             → MCP Google Calendar
أريد بيانات شركات      → MCP Business Enrichment
```
